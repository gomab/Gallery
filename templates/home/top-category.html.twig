<div class="row">
    {% for image in images %}
        <div class="col-sm-12 col-md-6">
            <a href="{{ path('detail') }}">
                <img src="images/{{ image }}" alt="..." class="img-thumbnail">
            </a>
        </div>
    {% endfor %}
</div>