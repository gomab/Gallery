{% extends 'base.html.twig' %}

{% block body %}
   <div class="Container">
       <h4>{{ post.title }} n° {{ slug }}</h4>
       <p>{{ post.content }}</p>
       <hr>
       <a href="" class="btn btn-primary">Lire plus ...</a>
   </div>
{% endblock %}