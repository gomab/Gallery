{% extends 'base.html.twig' %}


{% block body %}
    <div class="row">
        {% for image in images %}
            <div class="col-sm-6 col-md-4">
                <a href="">
                    <img src="build/images/{{ image }}" alt="..." class="img-thumbnail">
                </a>
            </div>
        {% endfor %}
    </div>
{% endblock %}
